import React from 'react';

const Loading = () => (
  <div>
    <i
      style={{ fontSize: "30px" }}
      class="fa fa-spinner fa-spin"
      aria-hidden="true"
    />
  </div>
);

export default Loading;