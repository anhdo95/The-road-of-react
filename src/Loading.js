import React from 'react';

const Loading = () => (
  <div>
    <i
      style={{ fontSize: "30px" }}
      className="fa fa-spinner fa-spin"
      aria-hidden="true"
    />
  </div>
);

export default Loading;